import { Variants } from "framer-motion";

export const defaultVariant = {
  initial: "initial",
  animate: "animate",
  exit: "exit",
};

export const defaultGesture: Variants = {
  whileTap: {
    opacity: 0.8,
    scale: 0.96,
  },
};
